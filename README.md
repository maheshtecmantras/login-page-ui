# login-page-ui

This repository hosts a single-page React experience that implements the polished login screen described in the Linear issue. It showcases a responsive hero section with supporting copy alongside an accessible login form.

## Getting started

1. `cd frontend`
2. `npm install`
3. `npm start`

The app runs locally on `http://localhost:3000` by default. Build for production with `npm run build`.
